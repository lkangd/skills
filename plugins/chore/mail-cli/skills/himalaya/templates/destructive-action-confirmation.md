Use only when confirmation is required (bulk/unseen moves, delete, expunge, purge, flag changes). Skip for post-read archive/move of the same ID(s) the user just asked you to expand — see `references/workflows.md`.

Action:   <move/delete/expunge/etc>
Account:  <acc>
Folder:   <source folder>
Target:   <destination folder if any>
IDs:      <comma-separated IDs>

Reply `confirm` to run this action, `edit` to change it, or `cancel` to abort.
