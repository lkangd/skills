Action:   <move/delete/expunge/etc>
Account:  <acc>
Folder:   <source folder>
Target:   <destination folder if any>
IDs:      <comma-separated IDs>

Reply `confirm` to run this action, `edit` to change it, or `cancel` to abort.
